# Golden Corridor README

The golden corridor is the public-safe proof path for the MACI-Elyria seam.

It contains valid input, refusal input, stale-evidence input, changed-scope input, bypass input, receipts, replay artifacts, no-bind transcript, and digest manifest.

Core invariant: MACI informs. Elyria resolves. No consequence binds without the boundary result.
